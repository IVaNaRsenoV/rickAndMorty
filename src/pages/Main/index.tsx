import { FC } from 'react';
import { Header, Input, Group } from '../../components';

export const Main: FC = () => {
  return (
    <>
        <Header />
        <Input />
        <Group />
    </>
  )
}
